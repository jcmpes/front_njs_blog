export const getAuth = (state) => state.auth;

export const getPostsData = (state) => state.posts.data;

export const getPostsLoaded = state => state.posts.loaded;

export const getToken = (state) => state.auth.token;